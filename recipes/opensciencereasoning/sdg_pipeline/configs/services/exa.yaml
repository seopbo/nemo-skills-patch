definition:
  name: Exa Search API via python
  description: Connection details and usage for Exa.

usage:
  language: python
  library: exa_py
  code: |
    import requests
    url = "https://api.exa.ai/search"
    headers = {"x-api-key": os.getenv("EXA_API_KEY")} # Initialize with API Key
    response = requests.post(url, headers=headers, json={"query": query, "numResults": max_results})
